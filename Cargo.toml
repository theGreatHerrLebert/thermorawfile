[package]
name = "thermorawfile"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust reader (and, soon, writer) for Thermo Finnigan .raw files — no .NET/DLL"
license = "MIT OR Apache-2.0"

[[bin]]
name = "rawdump"
path = "src/main.rs"

[lib]
name = "thermorawfile"
path = "src/lib.rs"
